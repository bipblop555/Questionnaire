<Page x:Class="Questionnaire.Views.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Questionnaire.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Questionnaire Manager" 
                   Margin="10"
                   FontSize="16"
                   FontWeight="Bold"/>

        <!-- List with Remove buttons -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl x:Name="QuestionnairesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Title}" 
                                       Margin="5" 
                                       VerticalAlignment="Center"/>
                            <Button Content="Remove" 
                                    Grid.Column="1"
                                    Margin="5"
                                    Click="RemoveQuestionnaire_Click"
                                    Tag="{Binding Id}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Add Button -->
        <Button Grid.Row="2" 
                x:Name="AddButton" 
                Content="Add New Questionnaire" 
                Margin="10"
                Click="AddQuestionnaire_Click"/>
    </Grid>
</Page>